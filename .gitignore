# macOS
.DS_Store

# Chat transcripts / extracted session notes
assets/*.txt

# Secrets — both contain the Anthropic API key
app/.env
app/local-key.js
.codex-staging/
.vercel

# Local-only demo risk review (names exploitable routes; not for the public repo)
DEMO-LIVE-RISKS.md
